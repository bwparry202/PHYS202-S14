#!/bin/bash

# This script configures git whenever you start working 
# on multiple machines. Run it anytime you decide to work with
# git on a new computer.

git config --global user.name "Bryce Parry"
git config --global user.email "bwparry@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor "nano"

